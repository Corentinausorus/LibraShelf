{% extends 'base.html.twig' %}

{% block title %}Administration — Tableau de bord{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="mb-5 text-center">
        <h1 class="display-5 fw-bold mb-3" style="color: #10b981;">
            <i class="bi bi-shield-lock-fill"></i> Administration
        </h1>
        <p class="lead text-muted">
            Panneau de contrôle administrateur — Gérez votre bibliothèque en toute simplicité
        </p>
    </div>

    <div class="row g-4">
        <div class="col-md-6">
            <div class="card h-100">
                <div class="card-body d-flex flex-column p-4">
                    <div class="d-flex align-items-center mb-3">
                        <div class="icon-circle me-3">
                            <i class="bi bi-people-fill" style="font-size: 2rem; color: #10b981;"></i>
                        </div>
                        <h5 class="card-title mb-0 fw-bold">Utilisateurs</h5>
                    </div>
                    <p class="card-text flex-grow-1 text-muted">
                        Gérez les utilisateurs et attribuez les rôles (Administrateur, Bibliothécaire, Membre).
                    </p>
                    <a href="{{ path('admin_users') }}" class="btn btn-primary mt-3">
                        <i class="bi bi-arrow-right-circle"></i> Gérer les utilisateurs
                    </a>
                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card h-100">
                <div class="card-body d-flex flex-column p-4">
                    <div class="d-flex align-items-center mb-3">
                        <div class="icon-circle me-3">
                            <i class="bi bi-journal-bookmark-fill" style="font-size: 2rem; color: #10b981;"></i>
                        </div>
                        <h5 class="card-title mb-0 fw-bold">Emprunts & Pénalités</h5>
                    </div>
                    <p class="card-text flex-grow-1 text-muted">
                        Consultez l'historique complet des emprunts et gérez les pénalités de retard.
                    </p>
                    <a href="{{ path('app_emprunt_index') }}" class="btn btn-primary mt-3">
                        <i class="bi bi-arrow-right-circle"></i> Voir les emprunts
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
