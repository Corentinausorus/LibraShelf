{% extends 'base.html.twig' %}

{% block title %}Administration — Tableau de bord{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="mb-4">
        <h1 class="h3"><i class="bi bi-speedometer2"></i> Tableau de bord admin</h1>
        <p class="text-muted">Bienvenue sur le panneau d'administration. Utilisez les cartes ci-dessous pour gérer le système.</p>
    </div>

    <div class="row g-3">
        <div class="col-md-6">
            <div class="card shadow-sm h-100">
                <div class="card-body d-flex flex-column">
                    <h5 class="card-title"><i class="bi bi-people"></i> Utilisateurs</h5>
                    <p class="card-text flex-grow-1">Gérez les utilisateurs et leurs rôles (Admin, Bibliothécaire, Membre).</p>
                    <a href="{{ path('admin_users') }}" class="btn btn-primary mt-auto">
                        <i class="bi bi-arrow-right-circle"></i> Accéder
                    </a>
                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card shadow-sm h-100">
                <div class="card-body d-flex flex-column">
                    <h5 class="card-title"><i class="bi bi-book"></i> Emprunts & Pénalités</h5>
                    <p class="card-text flex-grow-1">Consultez et gérez les emprunts, l'historique et les pénalités de retard.</p>
                    <a href="{{ path('app_emprunt_index') }}" class="btn btn-primary mt-auto">
                        <i class="bi bi-arrow-right-circle"></i> Accéder
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
