{% extends 'base.html.twig' %}

{% block title %}Administration — Tableau de bord{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h1 class="h3"><i class="bi bi-speedometer2"></i> Tableau de bord admin</h1>
        <a href="{{ path('admin_users') }}" class="btn btn-outline-primary">
            <i class="bi bi-people"></i> Gestion des utilisateurs
        </a>
    </div>

    <div class="card shadow-sm">
        <div class="card-body">
            <p class="mb-0">Bienvenue sur le panneau d'administration. Utilisez les actions ci-dessus pour gérer les utilisateurs et leurs rôles.</p>
        </div>
    </div>
</div>
{% endblock %}
