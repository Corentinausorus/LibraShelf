<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="LibraShelf - Système de gestion de bibliothèque moderne et intuitif">
    <title>{% block title %}LibraShelf{% endblock %}</title>
    
    {# Bootstrap Icons via CDN (font files not easily loaded via Asset Mapper) #}
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    
    {# Asset Mapper - loads Bootstrap CSS/JS and custom styles #}
    {% block importmap %}{{ importmap('app') }}{% endblock %}
    
    {% block stylesheets %}{% endblock %}
</head>
<body>
    {# MENU DE NAVIGATION PRINCIPAL #}
    {# Visible sur TOUTES les pages de l'application #}
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary sticky-top">
        <div class="container-fluid">
            {# Logo - Retour à l'accueil #}
            <a class="navbar-brand" href="{{ path('app_home') }}">
                <i class="bi bi-book"></i> LibraShelf
            </a>
            
            {# Bouton hamburger pour mobile #}
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                {# Menu principal - Adapté au rôle #}
                <ul class="navbar-nav me-auto">
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_dashboard') }}">
                                <i class="bi bi-shield-check"></i> Administration
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_users') }}">
                                <i class="bi bi-people"></i> Utilisateurs
                            </a>
                        </li>
                    {% endif %}
                    
                    {% if is_granted('ROLE_LIBRARIAN') %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('librarian_dashboard') }}">
                                <i class="bi bi-speedometer2"></i> Dashboard
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('librarian_catalog') }}">
                                <i class="bi bi-book-half"></i> Catalogue
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('librarian_loans') }}">
                                <i class="bi bi-arrow-left-right"></i> Emprunts
                            </a>
                        </li>
                    {% endif %}
                    
                    {% if is_granted('ROLE_MEMBER') %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('member_ouvrages') }}">
                                <i class="bi bi-search"></i> Rechercher
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('member_loans') }}">
                                <i class="bi bi-journal-bookmark"></i> Mes emprunts
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('member_reservations') }}">
                                <i class="bi bi-bookmark-star"></i> Mes réservations
                            </a>
                        </li>
                    {% endif %}
                </ul>
                
                {# Menu utilisateur (droite) #}
                <ul class="navbar-nav">
                    {% if app.user %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" 
                               data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="bi bi-person-circle"></i> {{ app.user.nom }}
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                                <li>
                                    <a class="dropdown-item" href="{{ path('app_profile') }}">
                                        Profil
                                    </a>
                                </li>
                                <li><hr class="dropdown-divider"></li>
                                <li>
                                    <a class="dropdown-item" href="{{ path('app_logout') }}">
                                        <i class="bi bi-box-arrow-right"></i> Se déconnecter
                                    </a>
                                </li>
                            </ul>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_login') }}">
                                <i class="bi bi-box-arrow-in-right"></i> Se connecter
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_register') }}">
                                <i class="bi bi-person-plus"></i> S'inscrire
                            </a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>

    {# CONTENU DES PAGES #}
    {% block body %}{% endblock %}
    
    {% block javascripts %}
        {# Bootstrap JS is now loaded via Asset Mapper in app.js #}
    {% endblock %}

    {# Footer moderne #}
    <footer class="site-footer">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-md-6 text-center text-md-start mb-3 mb-md-0">
                    <span class="footer-brand">
                        <i class="bi bi-book-fill"></i> LibraShelf
                    </span>
                    <span class="footer-copyright">© {{ "now"|date("Y") }} - Système de gestion de bibliothèque</span>
                </div>
                <div class="col-md-6 text-center text-md-end">
                    {% if app.user %}
                        <span class="footer-user">
                            <i class="bi bi-person-circle"></i> {{ app.user.userIdentifier }}
                            <span class="badge bg-primary ms-2">{{ app.user.getRoles()|first|replace({'ROLE_': ''}) }}</span>
                        </span>
                        <a href="{{ path('app_logout') }}" class="btn btn-sm btn-outline-light ms-3">
                            <i class="bi bi-box-arrow-right"></i> Déconnexion
                        </a>
                    {% else %}
                        <a href="{{ path('app_login') }}" class="btn btn-sm btn-primary">
                            <i class="bi bi-box-arrow-in-right"></i> Connexion
                        </a>
                    {% endif %}
                </div>
            </div>
        </div>
    </footer>
</body>
</html>